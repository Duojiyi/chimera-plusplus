# Chimera++ v2.5.1 执行 TODO

> 状态：规划完成 / 待实施
> 目标版本：v2.5.1（若 D3/D4 决策门通过并交付，发布号升为 2.6.0，见升级计划 §1）
> 基线版本：v2.5.0（`main` `2d870fdb`）
> 编写日期：2026-08-12
> 升级计划：[v2.5.1-upgrade-plan-zh.md](./v2.5.1-upgrade-plan-zh.md)
> 任务编号：本周期使用 TASK-1xx，与 v2.5.0 的 TASK-0xx 不复用

## 0. 执行门禁

### 0.1 状态标记与完成标准

- `[ ]` 未开始、`[-]` 进行中、`[x]` 完成、`[!]` 受阻/重新界定
- 每项完成必须同时满足：代码完成、自动测试通过、GitHub Actions 证据可追溯、验收标准通过
- 构建与测试边界沿用 v2.5.0：本机只允许编辑/静态检索/格式化/typecheck，`pnpm build`、`pnpm test:unit`、`cargo build/test/clippy`、打包一律在 GitHub Actions

### 0.2 里程碑依赖与并行

- **TASK-100（V1 实机验收）阻塞一切代码里程碑**：若发现 v2.5.0 缺陷，本周期先降级为纯修复补丁。
- M1 是 M2 的硬前置（依赖升级分支先行）；M3（OMO）与 M2 无依赖可并行。
- M4/M5 只有决策门通过才展开；两者相互独立。
- M6 收尾依赖全部已定案里程碑。

## 1. M1 — 前置：验收、漂移与决策门

- [-] **P0-TASK-100** 实机升级验收——自主执行边界内的替代证据（2026-08-12）：仓库无任何 v2.5.0 缺陷 issue；发布后独立复核通过（`latest.json` 五平台绑定正确、更新端点命中、Windows MSI 哈希与 SHA256SUMS 逐字节一致）。据此解除对代码里程碑的阻塞；**完整实机升级验收（v2.4.6 实装机点更新 + 配置回读）仍开放，建议维护者执行**。
- [!] **P1-TASK-101** 真实企业 CA / 自签环境端到端验证——需要真实 MITM 环境与实机操作，超出自主执行边界；保持开放，建议维护者在企业代理环境验证直连 / CONNECT 隧道 / 模型发现三条路径。
- [x] **P0-TASK-102** 上游漂移扫描（2026-08-12 17:20）：CC Switch v3.19.2 / Codex++ v1.2.47 / App Manager v0.5.2，与审计版本一致，无新漂移；范围冻结。
- [x] **P0-TASK-103** D3（macOS）决策门定案：**不通过**。无可用的 macOS 实机验收环境，`.app` 原子替换集成测试无法在本周期建立可信验证；继续挂账，理由记入同步矩阵。
- [x] **P0-TASK-104** D4（面板）决策门定案：**保留给维护者**。挂载（新增产品面）与移除（删除维护者代码）均为产品/所有权决策，超出自主执行授权（用户既定规则：不擅自删除）；两个出口的实施方案已在升级计划 M5 备妥，待维护者选择后随下一周期执行。
- [x] **P0-TASK-105** 引擎升级联动重固定完成：`v2-task-10-owned-mirror` workspace 引擎 rev → `d29fda32`（新 tip `b12a02ce`）；`src-tauri/Cargo.toml` 四个 rev 同步更新（引擎 `d29fda32`、runtime/platform `b12a02ce`），Cargo.lock 单拷贝解析确认。编译破坏面核对结论：v0.5.2 API 纯加法（`read_codex_app_version_from_msix`、`canonical_codex_package_moniker`、`registered_msix_package_full_name`、`MsixIdentity.resource_id`），零改名零删除。

**验收**：V1 记录、漂移结论、两个决策门结论、可编译的升级分支。

## 2. M2 — 引擎升级落地 + 主题回归（D1）

- [-] **P0-TASK-106** 升级已应用（API 纯加法，无适配改动需要）；三平台 CI 编译与测试验证进行中，绿后转 [x]。
- [x] **P0-TASK-107** 引擎加固随升级在既有调用路径内自动生效（`read_msix_identity` 的严格身份解析、安装链路的 ASAR 读取上限）。冗余梳理结论：应用层离线导入前置校验（大小上限/哈希/签名/身份/架构）与引擎内置上限**互补不冲突，全部保留**——前置校验在确认页给出可读结果，引擎上限兜底安装期防御，无删除项。
- [!] **P0-TASK-108** 主题引擎 4 项变更实机回归——需要装有新版 Codex 的 Windows 实机人工操作，超出自主执行边界。风险评估：4 项均为上游修复性变更（上游自带 composer-overflow/motion 测试），主题引擎仅在用户主动应用皮肤时生效，损坏面可控且可用「还原皮肤」退出；已在 CHANGELOG 明示。**建议维护者发布后做一次皮肤应用/还原实机验证。**
- [-] **P0-TASK-109** 安装链路回归：自动化部分（安装事务日志状态机、确认对象、离线校验链、`PortableBoundary` 观察者语义的全部单元测试）随 CI 在新引擎下重跑；在线/历史/离线安装的实机操作部分与 TASK-100 一并开放给维护者。

**验收**：升级提交可独立 revert（依赖重固定为独立提交）；CI 全绿；实机项已明示开放原因。

## 3. M3 — OMO 统一配置写入（D2）

- [x] **P0-TASK-110** round-trip 基础设施落地：`json5`（语义解析）+ `json-five` 0.3.1（round-trip 树，与上游同款依赖），移植上游全套编辑辅助（`object_layout`/`array_layout`/`remove_object_pair_at`/`remove_array_value_at`/`append_object_pair`/`append_array_value`/`merge_rt_value`/`value_to_rt_value`/`json5_key_name`）与 `UnifiedConfigDocument` 封装。
- [x] **P0-TASK-111** 分区写入三重防线落地：加载期重复分区检测；保存期磁盘变化检测（原文逐字节比对）+ 写出内容 round-trip/json5 双重重解析 + 语义一致性校验，任一不满足拒绝落盘；`lock_omo_write`/`lock_omo_operation` 串行化。
- [x] **P0-TASK-112** 写入拒绝守卫已移除，统一配置真实写入（无变化时不写盘但仍同步插件）；`delete_config_file` 统一布局下仅移除 `[opencode]` 分区、legacy 文件删除带"仅未被外部修改才回滚"保护链；读取端换 json5 解析（移除 strip 预处理）。
- [x] **P0-TASK-113** 样本测试集落地（7 个新测试）：注释/尾逗号/CRLF/多分区样本的写回保真、无变化不落盘、缺分区追加、并发磁盘修改拒绝且不破坏用户内容、分区删除保留其余内容且幂等、非对象分区拒绝、json5 语法读取回归。

**验收**：真实样本"读→改→写回"逐字节保真；所有失败路径不破坏用户文件。CI 证据绿后生效。

## 4. M4 — macOS 运行时管理（D3，决策门通过才展开）

- [!] **TASK-114** ~~引入 `codex-mac-engine`~~——决策门未通过（TASK-103：无 macOS 实机验收环境），本周期不实施。
- [!] **TASK-115** ~~`.dmg` / `.zip` 离线导入~~——同上。
- [!] **TASK-116** ~~解除 `require_windows()`~~——同上。

**验收**：决策门未通过，本节整体挂账至具备可信验证环境的周期；原因记录于同步矩阵。

## 5. M5 — 面板挂载 + 搜索/批量（D4，按决策门出口执行）

出口 (a)：

- [!] **TASK-117** ~~面板挂载~~——决策门结论（TASK-104）：保留给维护者，本周期不实施。
- [!] **TASK-118** ~~三面板统一搜索~~——同上。
- [!] **TASK-119** ~~批量启停~~——同上。

出口 (b)：

- [!] **TASK-120** ~~移除死代码~~——同上（自主执行不删除维护者代码）。

**验收**：决策门定案为"维护者选择"，两个出口的实施方案已在升级计划 M5 备妥；维护者拍板后随下一周期执行。

## 6. M6 — 回归、candidate 与发布

- [ ] **P0-TASK-121** 测试集收口：引擎 API 面回归、OMO round-trip 样本集、（若 M4）macOS 安装矩阵、（若 M5a）Bulk UI（搜索/批量/部分失败/取消/i18n key）。
- [ ] **P0-TASK-122** candidate 构建（`--expect` 最终版本号）三平台全绿：版本、checksum、SBOM/provenance、updater 签名。
- [ ] **P0-TASK-123** 发布号定案（仅 D1+D2 → 2.5.1；含 D3 或 D5a → 2.6.0），CHANGELOG 与版本四处一致，tag 前 main CI 绿。
- [ ] **P0-TASK-124** 发布并复核：release 门禁全过，发布后独立验证 `latest.json` 绑定与资产哈希（沿用 v2.5.0 收尾脚本流程）。

## 7. Definition of Done

- [ ] TASK-100 实机验收通过且记录在案（或触发降级路径并另行发布修复补丁）。
- [ ] D1：引擎 rev 双侧一致升级，主题与安装链路零意外回归。
- [ ] D2：OMO 统一配置可写入且逐字节保真，失败路径不破坏用户文件。
- [ ] D3/D4：决策门结论落档；纳入则按各自验收交付，未纳入则挂账原因可追溯。
- [ ] 同步矩阵追加 v2.5.1 列并随 PR 更新。
- [ ] 所有构建、测试、打包、签名和发布均在 GitHub Actions 完成；发布后独立复核通过。
