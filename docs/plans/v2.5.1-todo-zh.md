# Chimera++ v2.5.1 执行 TODO

> 状态：规划完成 / 待实施
> 目标版本：v2.5.1（若 D3/D4 决策门通过并交付，发布号升为 2.6.0，见升级计划 §1）
> 基线版本：v2.5.0（`main` `2d870fdb`）
> 编写日期：2026-08-12
> 升级计划：[v2.5.1-upgrade-plan-zh.md](./v2.5.1-upgrade-plan-zh.md)
> 任务编号：本周期使用 TASK-1xx，与 v2.5.0 的 TASK-0xx 不复用

## 0. 执行门禁

### 0.1 状态标记与完成标准

- `[ ]` 未开始、`[-]` 进行中、`[x]` 完成、`[!]` 受阻/重新界定
- 每项完成必须同时满足：代码完成、自动测试通过、GitHub Actions 证据可追溯、验收标准通过
- 构建与测试边界沿用 v2.5.0：本机只允许编辑/静态检索/格式化/typecheck，`pnpm build`、`pnpm test:unit`、`cargo build/test/clippy`、打包一律在 GitHub Actions

### 0.2 里程碑依赖与并行

- **TASK-100（V1 实机验收）阻塞一切代码里程碑**：若发现 v2.5.0 缺陷，本周期先降级为纯修复补丁。
- M1 是 M2 的硬前置（依赖升级分支先行）；M3（OMO）与 M2 无依赖可并行。
- M4/M5 只有决策门通过才展开；两者相互独立。
- M6 收尾依赖全部已定案里程碑。

## 1. M1 — 前置：验收、漂移与决策门

- [ ] **P0-TASK-100** 实机升级验收：装有 v2.4.6 的 Windows 机器执行应用内更新到 v2.5.0，验证供应商配置、模型映射（含 per-model 路由）、用量数据完好；结论记录到本文件与同步矩阵。
- [ ] **P1-TASK-101** 真实企业 CA / 自签环境端到端连接验证（G1 实机部分）：企业代理或本地 MITM（如 mitmproxy 自签 CA 装入系统信任）下验证直连、CONNECT 隧道、模型发现三条路径。
- [ ] **P0-TASK-102** 上游漂移扫描：对照 2026-08-12 审计（CC Switch v3.19.2 / Codex++ v1.2.47 / App Manager v0.5.2），有新发布则增量更新同步矩阵后再冻结范围。
- [ ] **P0-TASK-103** D3（macOS）决策门定案：评估 `codex-mac-engine` API 稳定性、macOS CI 承载能力、人工验收设备可用性；结论与依据写入同步矩阵。
- [ ] **P0-TASK-104** D4（面板）决策门定案：产品决策"挂载进 ChimeraApp"（出口 a）或"移除死代码"（出口 b）；结论写入升级计划 §0.1 与同步矩阵。
- [ ] **P0-TASK-105** 创建引擎升级分支：`src-tauri/Cargo.toml` 与 `v2-task-10-owned-mirror` workspace 同步指到 `d29fda32`（v0.5.2），列出编译破坏面清单（已知：msix 导出重组、`MsixIdentity.resource_id`、`read_codex_app_version_from_msix`）。

**验收**：V1 记录、漂移结论、两个决策门结论、可编译的升级分支。

## 2. M2 — 引擎升级落地 + 主题回归（D1）

- [ ] **P0-TASK-106** 适配 `89b542b9` → `d29fda32` API 变化，双侧（src-tauri + runtime 分支）rev 一致，三平台 CI 全绿。
- [ ] **P0-TASK-107** 采纳引擎加固：ASAR 读取上限、MSIX publisher-ID/`resource_id` 校验生效；离线导入的应用层前置校验与引擎内置上限做冗余梳理（保留不冲突者，删除的必须在提交说明中列出对应引擎守卫）。
- [ ] **P0-TASK-108** 主题引擎 4 项变更实机回归（Windows + 新版 Codex）：26.727 主界面、composer scroll ownership、composer overflow cache、CSP-safe motion intros；主题应用/还原不破坏输入区。
- [ ] **P0-TASK-109** v2.5.0 安装链路在新引擎下全量回归：在线安装、历史版本、离线导入、回滚、事务日志（`PortableBoundary` 语义不变）。

**验收**：升级提交可独立 revert；安装与主题行为相对 v2.5.0 零意外变化。

## 3. M3 — OMO 统一配置写入（D2）

- [ ] **P0-TASK-110** 移植 round-trip JSON5 编辑基础设施（`rt_from_str` / `merge_rt_value` / `append_object_pair` / `object_layout` / `remove_object_pair_at` + `json5` 解析），注释/缩进/行尾保持。
- [ ] **P0-TASK-111** `[opencode]` 分区写入：重复分区检测、磁盘变化检测（保存前原文比对）、写后重解析校验、`omo_write_lock` 串行化；失败保留原文件不落盘。
- [ ] **P0-TASK-112** 移除 v2.5.0 的统一配置写入拒绝守卫，替换为真实写入；`delete_config_file` 在统一布局下仅移除分区不删文件。
- [ ] **P0-TASK-113** round-trip 样本测试集：注释、尾逗号、CRLF、Unicode、深嵌套；断言非 `[opencode]` 内容逐字节不变；并发修改/非对象分区/重复分区显式失败。

**验收**：真实样本"读→改→写回"逐字节保真；所有失败路径不破坏用户文件。

## 4. M4 — macOS 运行时管理（D3，决策门通过才展开）

- [ ] **TASK-114** 引入 `codex-mac-engine`（同 rev 固定），实现检测/安装/更新/原子替换/健康检查/回滚。
- [ ] **TASK-115** `.dmg` / `.zip` 离线导入：大小上限 → SHA-256 → 签名/公证 → 身份 → 架构 → 版本，全部安装前完成。
- [ ] **TASK-116** 解除相关命令 `require_windows()`；事务日志、恢复横幅、确认对象复用 v2.5.0 基础设施；macOS CI 集成测试 + 实机人工验收。

**验收**：macOS 全链路可用且中断恢复语义与 Windows 一致；Windows 零回归。未通过决策门则本节整体标 `[!]` 并记录原因。

## 5. M5 — 面板挂载 + 搜索/批量（D4，按决策门出口执行）

出口 (a)：

- [ ] **TASK-117** MCP/Skills/Prompts 面板懒加载挂进 `ChimeraApp` 导航（含空状态与权限边界）。
- [ ] **TASK-118** 三面板统一客户端搜索（复用 SkillsPage 过滤范式）+ 无匹配空状态。
- [ ] **TASK-119** MCP/Skills 按应用批量启停：后端批量命令（合并每次调用的全量文件同步开销）、前端串行执行/失败继续/结果汇总；Prompts 按其单选语义仅批量禁用/删除。

出口 (b)：

- [ ] **TASK-120** 移除三面板 + `AuthCenterPanel` + `SettingsPage` 死代码及关联测试；同步矩阵将 G5 关闭为 `not-applicable`；全仓 typecheck 与测试通过。

**验收**：出口二选一执行完毕，另一出口任务标注不适用。

## 6. M6 — 回归、candidate 与发布

- [ ] **P0-TASK-121** 测试集收口：引擎 API 面回归、OMO round-trip 样本集、（若 M4）macOS 安装矩阵、（若 M5a）Bulk UI（搜索/批量/部分失败/取消/i18n key）。
- [ ] **P0-TASK-122** candidate 构建（`--expect` 最终版本号）三平台全绿：版本、checksum、SBOM/provenance、updater 签名。
- [ ] **P0-TASK-123** 发布号定案（仅 D1+D2 → 2.5.1；含 D3 或 D5a → 2.6.0），CHANGELOG 与版本四处一致，tag 前 main CI 绿。
- [ ] **P0-TASK-124** 发布并复核：release 门禁全过，发布后独立验证 `latest.json` 绑定与资产哈希（沿用 v2.5.0 收尾脚本流程）。

## 7. Definition of Done

- [ ] TASK-100 实机验收通过且记录在案（或触发降级路径并另行发布修复补丁）。
- [ ] D1：引擎 rev 双侧一致升级，主题与安装链路零意外回归。
- [ ] D2：OMO 统一配置可写入且逐字节保真，失败路径不破坏用户文件。
- [ ] D3/D4：决策门结论落档；纳入则按各自验收交付，未纳入则挂账原因可追溯。
- [ ] 同步矩阵追加 v2.5.1 列并随 PR 更新。
- [ ] 所有构建、测试、打包、签名和发布均在 GitHub Actions 完成；发布后独立复核通过。
